# Webhook Notifier

### Migrate database
`go run main.go migrate`
### Run APIs service
`go run main.go serve`
### Run Webhook Notifier
`go run main.go notifier`

### Run dependencies
`docker compose up -d`
